to run this project you need to install threejs and parcel
first npm init
using npm install threejs and parcel
then using parcel in the terminal excute this command
"parcel ./src/index.html" with the quotes
